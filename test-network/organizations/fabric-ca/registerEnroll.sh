#!/usr/bin/env bash

function createOrgregistrocivil() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/orgregistrocivil.example.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:7054 --caname ca-orgregistrocivil --tls.certfiles "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-orgregistrocivil.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-orgregistrocivil.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-orgregistrocivil.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-orgregistrocivil.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy orgregistrocivil's CA cert to orgregistrocivil's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem" "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/msp/tlscacerts/ca.crt"

  # Copy orgregistrocivil's CA cert to orgregistrocivil's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem" "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/tlsca/tlsca.orgregistrocivil.example.com-cert.pem"

  # Copy orgregistrocivil's CA cert to orgregistrocivil's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/ca"
  cp "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem" "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/ca/ca.orgregistrocivil.example.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-orgregistrocivil --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-orgregistrocivil --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-orgregistrocivil --id.name orgregistrociviladmin --id.secret orgregistrociviladminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:7054 --caname ca-orgregistrocivil -M "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates, use --csr.hosts to specify Subject Alternative Names"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:7054 --caname ca-orgregistrocivil -M "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/tls" --enrollment.profile tls --csr.hosts peer0.orgregistrocivil.example.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/peers/peer0.orgregistrocivil.example.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:7054 --caname ca-orgregistrocivil -M "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/users/User1@orgregistrocivil.example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/users/User1@orgregistrocivil.example.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://orgregistrociviladmin:orgregistrociviladminpw@localhost:7054 --caname ca-orgregistrocivil -M "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/users/Admin@orgregistrocivil.example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/orgregistrocivil/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/orgregistrocivil.example.com/users/Admin@orgregistrocivil.example.com/msp/config.yaml"
}

function createOrgcne() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/orgcne.example.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/orgcne.example.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:8054 --caname ca-orgcne --tls.certfiles "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-orgcne.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-orgcne.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-orgcne.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-orgcne.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/orgcne.example.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy orgcne's CA cert to orgcne's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/orgcne.example.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem" "${PWD}/organizations/peerOrganizations/orgcne.example.com/msp/tlscacerts/ca.crt"

  # Copy orgcne's CA cert to orgcne's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/orgcne.example.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem" "${PWD}/organizations/peerOrganizations/orgcne.example.com/tlsca/tlsca.orgcne.example.com-cert.pem"

  # Copy orgcne's CA cert to orgcne's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/orgcne.example.com/ca"
  cp "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem" "${PWD}/organizations/peerOrganizations/orgcne.example.com/ca/ca.orgcne.example.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-orgcne --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-orgcne --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-orgcne --id.name orgcneadmin --id.secret orgcneadminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:8054 --caname ca-orgcne -M "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/orgcne.example.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates, use --csr.hosts to specify Subject Alternative Names"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:8054 --caname ca-orgcne -M "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/tls" --enrollment.profile tls --csr.hosts peer0.orgcne.example.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/orgcne.example.com/peers/peer0.orgcne.example.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:8054 --caname ca-orgcne -M "${PWD}/organizations/peerOrganizations/orgcne.example.com/users/User1@orgcne.example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/orgcne.example.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/orgcne.example.com/users/User1@orgcne.example.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://orgcneadmin:orgcneadminpw@localhost:8054 --caname ca-orgcne -M "${PWD}/organizations/peerOrganizations/orgcne.example.com/users/Admin@orgcne.example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/orgcne/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/orgcne.example.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/orgcne.example.com/users/Admin@orgcne.example.com/msp/config.yaml"
}

function createOrderer() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/ordererOrganizations/example.com

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/ordererOrganizations/example.com

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:9054 --caname ca-orderer --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy orderer org's CA cert to orderer org's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem" "${PWD}/organizations/ordererOrganizations/example.com/msp/tlscacerts/tlsca.example.com-cert.pem"

  # Copy orderer org's CA cert to orderer org's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem" "${PWD}/organizations/ordererOrganizations/example.com/tlsca/tlsca.example.com-cert.pem"

# Loop through each orderer (orderer, orderer2, orderer3, orderer4) to register and generate artifacts
  for ORDERER in orderer orderer2 orderer3 orderer4; do
    infoln "Registering ${ORDERER}"
    set -x
    fabric-ca-client register --caname ca-orderer --id.name ${ORDERER} --id.secret ${ORDERER}pw --id.type orderer --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
    { set +x; } 2>/dev/null

    infoln "Generating the ${ORDERER} MSP"
    set -x
    fabric-ca-client enroll -u https://${ORDERER}:${ORDERER}pw@localhost:9054 --caname ca-orderer -M "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
    { set +x; } 2>/dev/null

    cp "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml" "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/msp/config.yaml"

    # Workaround: Rename the signcert file to ensure consistency with Cryptogen generated artifacts
    mv "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/msp/signcerts/cert.pem" "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/msp/signcerts/${ORDERER}.example.com-cert.pem"

    infoln "Generating the ${ORDERER} TLS certificates, use --csr.hosts to specify Subject Alternative Names"
    set -x
    fabric-ca-client enroll -u https://${ORDERER}:${ORDERER}pw@localhost:9054 --caname ca-orderer -M "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/tls" --enrollment.profile tls --csr.hosts ${ORDERER}.example.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
    { set +x; } 2>/dev/null

    # Copy the tls CA cert, server cert, server keystore to well known file names in the orderer's tls directory that are referenced by orderer startup config
    cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/tls/tlscacerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/tls/ca.crt"
    cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/tls/signcerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/tls/server.crt"
    cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/tls/keystore/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/tls/server.key"

    # Copy orderer org's CA cert to orderer's /msp/tlscacerts directory (for use in the orderer MSP definition)
    mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/msp/tlscacerts"
    cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/tls/tlscacerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/${ORDERER}.example.com/msp/tlscacerts/tlsca.example.com-cert.pem"
  done

  # Register and generate artifacts for the orderer admin
  infoln "Registering the orderer admin"
  set -x
  fabric-ca-client register --caname ca-orderer --id.name ordererAdmin --id.secret ordererAdminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the admin msp"
  set -x
  fabric-ca-client enroll -u https://ordererAdmin:ordererAdminpw@localhost:9054 --caname ca-orderer -M "${PWD}/organizations/ordererOrganizations/example.com/users/Admin@example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml" "${PWD}/organizations/ordererOrganizations/example.com/users/Admin@example.com/msp/config.yaml"
}
